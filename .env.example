# Future production database connection.
# Do not commit real credentials. Production secrets should be managed by the approved DevOps/security process.
DATABASE_URL="mysql://user:password@host:3306/ops_command_platform"
